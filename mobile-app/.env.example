# =============================================================================
# OM_Media Mobile（产品界面仍为「恒频OM」）— API 根地址（与 user-web 逻辑一致，无尾斜杠）
# 复制为 .env 后由 Expo 自动加载；也可启动时临时导出环境变量。
# 若 .env 中含明显占位符，开发模式会忽略并回退到代码内默认（见 src/config/env.ts）。
#
# 切勿填写文档里的占位词（如「你的后端」、your-host），否则请求会失败。
# =============================================================================

# --- A) 仓库根目录 docker-compose.local-beta（Nginx 用户站 :8080，API 未映射到宿主机 :8000）---
# 与本机浏览器访问 http://127.0.0.1:8080 时同源 API；模拟器见下行注释。
# EXPO_PUBLIC_API_BASE_URL=http://127.0.0.1:8080/api/v1
# Android 模拟器访问宿主机服务常用：
# EXPO_PUBLIC_API_BASE_URL=http://10.0.2.2:8080/api/v1

# --- B) backend/docker-compose.yml（API 映射 127.0.0.1:8000，与本仓库后端开发常见一致）---
EXPO_PUBLIC_API_BASE_URL=http://127.0.0.1:8000/api/v1

# --- C) 真机（同 Wi‑Fi）连你电脑上的后端：填局域网 IP + 端口（8080=local-beta Nginx，8000=direct api）---
# EXPO_PUBLIC_API_BASE_URL=http://192.168.1.100:8080/api/v1
# EXPO_PUBLIC_API_BASE_URL=http://192.168.1.100:8000/api/v1

# --- 登录 ---
# 与 Web 相同：POST /auth/login（用户名或邮箱 + 密码）。
