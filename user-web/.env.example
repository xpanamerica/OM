# 后端 API 根路径（含 /api/v1），无尾斜杠。
#
# 推荐（与 deploy/local-beta 用户站 :8080 + Nginx 反代一致）：
VITE_API_BASE_URL=http://127.0.0.1:8080/api/v1
#
# 若仅启动 backend/docker-compose 中的 api（:8000），改为：
# VITE_API_BASE_URL=http://127.0.0.1:8000/api/v1

# --- 外部浏览器 / 其他电脑访问本机 user-web（局域网）---
# 1) 后端监听 0.0.0.0:8000（勿只绑 127.0.0.1）
# 2) 将下面改为「运行后端的这台电脑」的局域网 IP（勿用 127.0.0.1，否则别人浏览器会打到自己手机）
#    VITE_API_BASE_URL=http://192.168.1.100:8000/api/v1
# 3) 启动前端并监听全网卡（任选其一）：
#    npm run dev:host
#    或：npm run dev -- --host 0.0.0.0
# 4) 其他设备浏览器打开：http://192.168.1.100:5173/auth  登录（与站点相同账号）

# Capacitor 原生壳：生产构建建议使用绝对 HTTPS URL，见 .env.capacitor.example 与 docs/CAPACITOR_20.2.md
